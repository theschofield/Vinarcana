# Lens session kickoff — paste everything below this line into a fresh session

---

I'd like to use this session to refine and complete the LENSES layer in the content folder,
running the same cycle that completed all 78 spirit voices on Jul 15, 2026 (voice-history.md,
"in-app refinement pass" batches 1–12).

**Read first, in order:**
1. CLAUDE.md, then `content/arcana-content.md` (the map of the pipeline).
2. `content/voice-history.md` — the calibration file, ESPECIALLY the twelve voice-pass batches
   dated 2026-07-15. Every law from that session transfers to lenses: feel-the-card, one stance
   per line, flag-then-decide on echoes, plain vocabulary only, no hedge words, the cold-read /
   polysemy check, recruit-don't-caution, mercy-that-respects-the-weight, the ring-true test,
   and the two grade questions ("What am I supposed to feel here? What is my reading?").
3. `content/voice-prompt.md` — Part 2 (the lens prompt) PLUS Part 1's drafting mindset and hard
   bans, which apply to every layer.
4. `docs/Tarot & Wine_ The Complete Framework.md` — every lens maps 1:1 (invisibly!) to a wine
   idea in here. The mapping is the whole point of the layer.
5. `content/lenses.csv` (current state), `content/spirit-voices.csv` (all 78 voices are now
   APPROVED canon — the lens set renders directly under its card's voice line, so check harmony
   and collisions against it), and `content/guidebook.csv` (the shared knowledgebase: sharing
   knowledge is fine, near-verbatim payoff reuse is banned, obvious construction twins get
   flagged for my call).

**THE AUDIT (before writing anything):** many cards are missing lenses — a lot of them have only
3 when the framework holds more pairing ideas (a missing 4th lens is common). For every card,
count the wine ideas in the framework against the card's lens rows and produce the gap list
(card · lens count · framework idea count) so we know the full scope. Fold the gap-filling into
the batches.

**The cycle (same as the voice pass):**
- Batches of 5 cards plus retries from the prior round. For each card: check every existing lens
  against Part 2's rules (name ≤ ~26 chars, written as an invitation, prefer "A" over "The";
  subtitle = a STATEMENT I can agree with, never a prescription of what I feel or do, steering
  toward the hidden wine without ANY wine word — grape, glass, vintage, farm, cellar, producer,
  region; exactly ONE nudge per card, on the strongest pairing story). Critique, propose fixes
  and the missing lenses, give a single recommendation per decision, then STOP for grading.
- The five approved sets (The Moon, Death, The Tower, The Fool, The Hermit) are canon: study
  them first, never edit them.
- A lens set must read as facets of ONE card: no two lenses blurring together, and the set has
  to sit naturally under the card's approved voice line on screen.
- As verdicts land: update `lenses.csv` (pending → draft → approved), log every graded draft in
  `voice-history.md` under the card tagged `- lens rN:`, and propagate every new law into
  `voice-prompt.md` Part 2.
- Never write reveals for unapproved lenses. Track which cards' lens sets reach fully approved —
  each one unlocks that card's guidebook closing (73 pending) for a later session.

We calibrated the voice pass to near-auto-pilot by the end. Start at that altitude: single
recommendations, stones turned only where the call is genuinely mine.
